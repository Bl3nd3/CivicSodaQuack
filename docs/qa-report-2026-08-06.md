# CivicSodaQuack — Exploratory QA Session Report

**Date:** 2026-08-06 (revised same day — see *Revision history*)
**Build under test:** `v0.7.0-1-g35515b0` (main @ `35515b0`)
**Platform:** macOS (darwin 25.5.0), arm64
**Method:** Unscripted end-to-end exercise — first-time MCP install, catalog discovery on an unfamiliar portal, config authoring, full sync, and analytical querying.

**Defect status:** 4 of 7 fixed (P1-1, P1-2, P2-1, P2-2). Both High-severity defects are closed. Remaining: P3-1, P3-2, P3-3, plus recommendations 3 and 4b.

---

## Executive summary

The core product works. A first-time operator took `csq` from a cold install to 3.17M rows of live county data queryable through an AI agent in a single session, with zero sync failures and no code changes required to target a portal the tool had never seen. The portal-agnostic architecture is the standout result — see Successes.

Seven defects surfaced. **Four are now fixed** — both High-severity ones (P1-1 `a100a6a`, P1-2 `95d56b3`) plus P2-1 (`68ad3f2`) and P2-2 (`c7def9c`). What remains is P3-1/2/3, all minor, plus two open recommendations.

The findings are not the most useful part of this report. Every one of the four fixes changed something the report itself asserted: P1-1's impact was overstated and its severity dropped to Medium, P2-1's benefit was narrower than claimed, and P1-2 and P2-2 both turned out to be **encoded in the test suite as intended behaviour** rather than merely missed by it. Those corrections are recorded in place rather than quietly edited away — see *Revision history*. A report that shows where it was wrong is worth more than one that reads as though it never was.

---

## Scope exercised

| Area | Exercised | Result |
|---|---|---|
| `claude mcp add` registration, stdio transport | ✅ | Pass |
| MCP `initialize` / `tools/list` handshake | ✅ | Pass |
| `csq catalog` against an unfamiliar portal | ✅ | Pass (1 defect) |
| YAML authoring, `include: - id:` selectors | ✅ | Pass |
| `sync --dry-run` validation | ✅ | Pass (1 defect) |
| `sync` — 5 datasets, 3.17M rows, concurrency 4 | ✅ | Pass |
| `mode: full_replace`, `table:` overrides | ✅ | Pass |
| Multi-portal `--db alias=path` | ✅ | Pass (was blocked by P1-1; fixed) |
| `query_sql` read-only enforcement | ✅ | Pass |
| Portal locking | ⚠️ | Detection good, remedy broken (P1-1) |
| Snapshot / fetch (Phase 4) | ❌ | Not exercised |
| Write tools (`sync_dataset`, `refresh_catalog`) | ❌ | Not exercised |
| HTTP transport | ❌ | Not exercised |

---

## Successes

**1. Portal-agnostic design held up under a real test.**
The single most valuable result. The session began against `data.cityofchicago.org`, determined mid-session that Chicago publishes no incarceration data, and pivoted to `datacatalog.cookcountyil.gov` — a portal the tool had never been pointed at. Discovery, config, sync, and MCP serving all worked with **no code changes and no per-portal special-casing**. This is the product thesis, and it is validated.

**2. Sync was clean at scale.**

```
[csq] summary: 5 ok, 0 failed, 0 aborted, wall 10m38.400382417s
```

| table | rows | expected | match |
|---|---:|---:|:--:|
| `initiation` | 1,228,260 | 1,228,260 | ✅ |
| `dispositions` | 1,080,014 | 1,080,014 | ✅ |
| `intake` | 528,111 | 528,111 | ✅ |
| `sentencing` | 305,884 | 305,884 | ✅ |
| `diversion` | 29,421 | 29,421 | ✅ |
| **total** | **3,171,690** | | |

Every table matched its upstream SODA `count(*)` exactly. Concurrency worked — datasets interleaved in the log and the 10m38s wall clock is roughly the longest single dataset, not the sum.

**3. `query_sql` read-only enforcement is well-designed.**
Wrapping user SQL in `BEGIN TRANSACTION READ ONLY` on an in-memory host with portals `ATTACH`ed `(READ_ONLY)` is the right call — engine-enforced rather than regex-filtered, which is the usual (and bypassable) approach. `tools_query_sql.go:37` documents the reasoning clearly.

**4. Sensible guardrails.**
`assertIsCSQDB` (`pools.go:108`) rejects arbitrary DuckDB files with a clear message. Result caps (1000 rows / 1MB / 30s) are appropriate for agent consumption. `${ENV}` expansion fails loudly rather than silently sending an empty token.

**5. Lock *detection* works correctly.**
The advisory `flock` fired exactly as designed and named the offending file. Only the suggested remedy is wrong (P1-1).

---

## Defects

### P1-1 — MCP server takes an exclusive *write* lock on read-only portals — ✅ FIXED (scope corrected)

**Severity:** High as filed. Downgraded on measurement to **Medium** — see *Resolution*, which corrects this entry's central impact claim.
**Status:** Fixed in `a100a6a`.

**Repro:**
1. `csq mcp --db chicago=data.cityofchicago.org.duckdb` (read-only; no `--config`)
2. In another shell, any second process touching that file:

```
csq mcp: portal database is locked by another process:
  data.cityofchicago.org.duckdb.lock
  (pass --no-lock to bypass, or --lock-wait <duration> to retry)
```

3. Follow the advice — `--no-lock` — and it fails anyway, worse:

```
IO Error: Could not set lock on file "data.cityofchicago.org.duckdb":
Conflicting lock is held in .../csq (PID 28092) by user jasper.
However, you would be able to open this database in read-only mode...
```

**Root cause:** `internal/mcpserver/pools.go`. Line 61 correctly `ATTACH`es each portal `(READ_ONLY)` to the in-memory host. But line 50 *also* opens a separate per-portal handle via `openDB(spec.Path)`, and `openDB` (line 95–105) calls `sql.Open("duckdb", path)` with **no `access_mode`** — i.e. read-write. That handle takes DuckDB's exclusive file lock.

The code comment at `pools.go:91–94` is explicit that this is read-write, and `pools.go:14–18` notes the per-portal pools "only run hardcoded internal queries (list/describe/search)" — all reads. So the write access is not needed for the read-only configuration.

**Impact:**
- Cannot `csq sync` a portal while an MCP server serves it — which is the steady state once an agent is configured.
- Cannot run two MCP servers over the same portal (e.g. one stdio for a local agent, one HTTP for a remote one).
- `--no-lock` is actively misleading: it skips `csq`'s *advisory* lock but DuckDB's lock sits underneath, so the flag cannot do what its error message promises. An operator following the hint gets a more confusing failure than if they hadn't.

**Suggested fix:** open the per-portal pool with `access_mode=read_only` when that portal has no `--config` (i.e. no write tools), matching the `ATTACH` mode. DuckDB permits unlimited concurrent cross-process readers, so this makes the common case fully concurrent. Where `--config` *is* supplied, read-write is genuinely required and the current lock behaviour is correct.

**Secondary fix:** amend the lock error message so it does not recommend `--no-lock` for a case that flag cannot resolve. *(Still open.)*

#### Resolution — `a100a6a`

`OpenPools` now takes the set of aliases needing write access and opens everything else `access_mode=read_only`. Only portals with a registered config are read-write: their write tools reopen the same path in-process via `duckdb.Open`, and DuckDB refuses to hold one file at two access modes. Read-only portals never needed write access — those pools only run hardcoded read queries.

**Correction — the first Impact bullet was wrong.** This entry claimed the fix would let a `csq sync` run while a portal is served. It does not, and implementing it proved so. Measured directly, DuckDB's cross-process rule is:

| holder | second opener | result |
|---|---|---|
| read-only | read-write | **blocked** |
| read-write | read-only | **blocked** |
| read-only | read-only | **succeeds** |

Many readers **or** one writer, never both. A writer is refused whichever mode the reader holds, so *serve-while-syncing is not reachable by changing access modes at all.* It needs snapshot-and-swap, or syncing to a second file and switching. That is a DuckDB constraint, not a csq defect, and the original entry mistook one for the other.

**What the fix does deliver** is the second bullet: reader-reader concurrency. Two servers over one portal — stdio for a local agent, HTTP for a remote one — or a server plus a `duckdb -readonly`/`csq query` session, all now work where the exclusive write lock previously refused them. Least-privilege is a real secondary benefit: the server no longer requests write access it cannot use.

**Why the original evidence looked contradictory.** csq embeds **DuckDB v1.4.3**; the standalone CLI installed later is **v1.5.5**, which is stricter about reader/writer overlap. That version gap is why a `duckdb -readonly` session attaches successfully to a file the running server holds read-write, while the same pairing between two v1.5.5 processes is refused. Any future concurrency claim should state the engine version it was measured on.

**Severity downgraded to Medium.** The blocked workflow that justified "High" — sync while serving — turns out to be unreachable regardless. What remains is real but narrower.

**Regression tests.** `TestOpenPools_ReadOnlyByDefault` asserts the engine actually refuses a write rather than trusting the struct flag. `TestOpenPools_ReadOnlyAllowsSecondProcess` re-executes the test binary to prove a second process can attach; it must be cross-process, and must seed out-of-process too, because DuckDB's instance cache keeps a file locked for the lifetime of whichever process opened it read-write — seeding in-process left the parent holding the very lock the test existed to disprove.

---

### P1-2 — `list_datasets` fabricates `table_name` for never-synced datasets — ✅ FIXED

**Severity:** High — hallucination vector in an agent-facing tool.

**Repro:** attach a portal DB that has had `csq catalog` run but never `csq sync`, then call `list_datasets`.

Observed on the Chicago DB: **2,010 entries returned, every one with a populated `table_name`** — against **zero** actual data tables. Only three `_csq` bookkeeping tables existed, and `_csq.sync_runs` was empty, confirming no sync had ever run.

**Root cause:** `internal/mcpserver/tools_list_datasets.go:113–118`:

```go
if table.Valid {
    summary.TableName = table.String
} else {
    summary.TableName = strings.ReplaceAll(id, "-", "_")   // ← invented
}
```

When the `LEFT JOIN` against `_csq.sync_runs` misses, the code synthesises a plausible-looking table name from the dataset ID. The one field that would betray this — `row_count` — is declared `json:"row_count,omitempty"` (line 25), so a `NULL` count is **omitted from the JSON entirely** rather than emitted as `null`.

**Impact:** The tool description reads "List datasets **available** across attached portal DuckDB files." An LLM agent receives 2,010 entries each carrying a concrete `table_name` and no contradicting signal, and will confidently emit `SELECT * FROM dpt3_jri9` — which fails with a binder error. This is precisely the failure mode an agent-facing data tool must not have: it is not a missing feature, it is the tool asserting something false. During this session it caused a real detour before the discrepancy was caught by cross-checking `duckdb_tables()`.

**Suggested fix:** emit `table_name: null` (or omit the entry) when no successful sync exists, and add an explicit `synced: bool`. Drop `omitempty` from `row_count` so `null` is visible. Consider a `synced_only` filter defaulting to `true`.

#### Resolution — `95d56b3`

`TableName` and `RowCount` became `*string`/`*int64` emitted without `omitempty`, so both appear as explicit `null`, and a new `synced` bool states the fact directly. Verified live: listing Cook County returns 808 datasets with exactly **5** non-null `table_name` values, matching the 5 tables actually synced.

**The same bug was present in `describe_dataset`, and worse.** It synthesised the name at `tools_describe_dataset.go:174-175` and then passed it to `readColumns`, probing `information_schema` for a table that was never created. That path now returns an empty column list when the dataset is unsynced.

**How it survived: the suite asserted it.** `TestListDatasets_NeverSynced_RowCountNil` explicitly checked for the synthesised fallback, under the comment `// Fallback table name from id`. This is a different failure from P2-1's fixture drift — there the test was wrong about the *world*, here it was wrong about the *requirement*, and no amount of running it would ever have surfaced the problem. Replaced with a test asserting the absence, plus one pinning the wire format so `table_name` and `row_count` are present-and-null rather than omitted.

---

### P2-1 — `_csq.catalog.updated_at` is always NULL (wrong JSON key) — ✅ FIXED

**Severity:** Medium — silent data loss; masked by a passing test.
**Status:** Fixed in `68ad3f2`. See *Resolution* below, which includes a correction to this entry's original impact assessment.

**Evidence:** across both portals, every catalog row has a NULL `updated_at`:

| portal | catalog rows | non-null `updated_at` |
|---|---:|---:|
| Chicago | 2,010 | **0** |
| Cook County | 808 | **0** |

The CLI reflects it — `csq catalog` printed `-` in the `UPDATED` column for every row on both portals.

**Root cause:** `internal/socrata/catalog.go:80` declares:

```go
RowsUpdatedAt string `json:"rowsUpdatedAt"`
```

The live `/api/catalog/v1` resource object contains **no `rowsUpdatedAt` key**. Actual keys are `updatedAt` and `data_updated_at`:

```
resource keys: [... 'createdAt', 'data_updated_at', ... 'metadata_updated_at', 'name', ... 'updatedAt']
  'rowsUpdatedAt': None
  'updatedAt':       2026-08-06T20:24:56.000Z
  'data_updated_at': 2026-08-06T20:24:56.000Z
```

The parse block at `catalog.go:135–138` is correct; it is simply never reached because the tag never matches.

**Why the test suite misses it:** `internal/socrata/catalog_test.go:29` builds a hand-written fixture containing `"rowsUpdatedAt": "2024-01-15T00:00:00.000"`, and lines 63–66 assert `UpdatedAt` is non-nil. `TestFetchCatalog_Paginates` **passes**. This is fixture drift — the test validates the parser against a response shape the API does not produce.

**Impact:** `updated_at` is the natural field for deciding what to re-sync and for surfacing dataset staleness — it was unavailable for either. Note `row_count` being NULL is *by design* (documented at `catalog.go:22`), so it is not a defect; `updated_at` is.

**Suggested fix:** change the tag to `data_updated_at` (preferred — it tracks data rather than metadata edits) or `updatedAt`. Add a smoke test that runs against a recorded *real* API response rather than a hand-authored fixture.

#### Resolution — `68ad3f2`

`rawCatalogEntry` now reads `data_updated_at`, falling back to `updatedAt` and then `rowsUpdatedAt` (so a `/api/views/` payload still resolves) through a new `parseCatalogTimestamp` helper.

**The field choice turned out to matter more than the bug.** Measured across a 100-dataset Chicago sample:

| comparison | count |
|---|---:|
| `updatedAt` == `data_updated_at` | 52 |
| `updatedAt` **later than** `data_updated_at` | 48 |
| `updatedAt` earlier | 0 |

`updatedAt` is `max(data_updated_at, metadata_updated_at)`. Dataset `2ft4-4uik` has data frozen since **2011** but an `updatedAt` of **2015**, purely because someone edited its description. The obvious one-word fix — pointing the tag at `updatedAt` — would have cleared the NULLs while silently reporting metadata churn as data freshness. Worth noting as a case where the cheap fix was the wrong one.

**Verified live, not only in tests** (the original test also passed while broken):

| portal | before | after |
|---|---:|---:|
| Cook County `_csq.catalog.updated_at` populated | 0 / 808 | **808 / 808** |

**Fixture drift addressed at the cause.** The fixture now mirrors a real payload. `TestCatalogTimestampPrecedence` pins the field order so a reorder fails loudly, and `TestFetchCatalog_LiveResponseShape` replays a trimmed real response including the frozen-dataset case.

**Correction to this entry's original impact claim.** The version of this report issued before the fix asserted that a working `updated_at` "would have immediately flagged that all five Cook County datasets were frozen." **That was wrong, and the fix disproves it.** Those five datasets were abandoned by the SAO on 2024-12-30 with coverage ending 2024-11-30, yet now report `updated_at` of **2026-04-02** — the portal republished unchanged rows and the timestamp moved with it.

So the fix closes the metadata-edit hole but not the republish hole. `data_updated_at` is a real improvement and the correct field, but it is still not a reliable staleness signal on its own; the dataset's own description remains the only dependable source. This narrows P2-1's practical benefit from what was originally claimed, and the README caveat was rewritten to match rather than left overstating the outcome.

---

### P2-2 — `list_datasets` has no pagination or result cap — ✅ FIXED

**Severity:** Medium — agent ergonomics.

**Repro:** call `list_datasets` with no filters on a 2,010-entry catalog.

**Observed:** a **351,820-character** response — large enough to exceed the agent's tool-result budget outright. `ListDatasetsArgs` (`tools_list_datasets.go:14`) exposes only `portal` and `category`; there is no `limit` or `offset`.

**Impact:** the tool is unusable in its default, no-argument form on any real portal — which is exactly how an agent will first call it. Notably `query_sql` *is* capped at 1000 rows / 1MB; `list_datasets` should inherit the same discipline.

**Suggested fix:** add `limit` (default ~100) and `offset`, and return a `total` count so the agent can page deliberately.

#### Resolution — `c7def9c`

`limit` and `offset` added to both `list_datasets` and `search_datasets`, defaulting to 100 and clamping at 1000 rather than erroring, so an over-eager caller still gets a usable answer. The response carries `total`, `returned`, `offset`, and `limit`, letting a consumer distinguish a complete answer from the first page of a long one.

Verified live against Cook County: the default no-argument call drops from **351,820 to 40,550 characters** (8.7x), and `offset=800&limit=3` returns the expected tail of 808 matches.

Tool descriptions were also updated to state the paging behaviour and that `table_name` is non-null only when `synced` is true — for an MCP server the tool description is the only documentation a consumer ever reads, so leaving it stale would have half-undone both this fix and P1-2.

---

### P3-1 — `--dry-run` does not enumerate what it would sync

**Observed:** the entire output for a five-dataset config was:

```
[csq] dry-run: would sync 5 datasets
```

**Impact:** the count confirms selectors resolved but not *which* datasets matched — the main thing a dry run should answer. With glob selectors (`category:`, `tag:`) this gap is more consequential, since a mistyped pattern silently matches the wrong set.

**Suggested fix:** list id, resolved table name, and mode per dataset.

---

### P3-2 — no `--version` flag

**Observed:** `./csq --version` → `unknown subcommand "--version"`.

The version is compiled in and reachable (`serverInfo.version` reported `v0.7.0-1-g35515b0` over MCP), but there is no way to read it from the CLI. This matters for bug reports — establishing the build under test required an MCP handshake.

**Suggested fix:** `csq --version` / `csq version`.

---

### P3-3 — unset `${ENV}` in `app_token` is a hard validation failure

**Observed:**

```
csq sync: validate "...yaml": app_token: ${ENV} reference expanded to
empty string (env var unset?)
```

Failing loudly is defensible and the message is good. But since anonymous access is legitimate (just rate-limited), a config committed with `app_token: ${SOCRATA_APP_TOKEN}` cannot be run by a colleague who hasn't set the variable — the workaround is to comment the line out, which is easy to commit by accident.

**Suggested fix:** consider a warn-and-continue path, or document `app_token: ${SOCRATA_APP_TOKEN:-}` style optional syntax.

---

## Operator errors (not product defects)

Recorded for completeness, and to keep the defect list honest:

| What happened | Cause |
|---|---|
| Lost streaming sync progress | Piped `csq sync` through `tail`, which buffers. Tool behaviour correct. |
| Misreported dataset sizes initially | Read Socrata's `viewCount` (page views) as a row count. Corrected via SODA `count(*)`. |
| Helper script crashed | Backslash inside an f-string expression in throwaway tooling. |
| Expected `--no-lock` to grant access | Reasonable inference from the error text — but the misleading hint is logged as P1-1. |

---

## Recommendations

**Before wider MCP rollout**
1. ~~Fix **P1-1** — open read-only portals `access_mode=read_only`.~~ **Done — `a100a6a`.** The stated rationale was wrong, though: this does *not* unlock concurrent sync + serve, which DuckDB forbids outright. It unlocks concurrent *readers*. If serve-while-syncing is genuinely wanted, that is a separate design item — sync to a second file and swap, which the Phase 4 snapshot machinery already has most of the pieces for.
2. ~~Fix **P1-2** — stop inventing `table_name`; add `synced`.~~ **Done — `95d56b3`.**
3. Correct the lock error message so it stops recommending a flag that cannot help. *(Still open; the fix in `a100a6a` did not touch `portallock`.)*

**Near term**
4. ~~Fix **P2-1** (`data_updated_at`) and re-point the catalog test at a recorded real response.~~ **Done — `68ad3f2`.** The remaining half of this item still stands: audit the other struct tags in `internal/socrata` against the live API. Where one field name drifted, others may have, and the same class of fixture would hide them equally well.
5. ~~Add `limit`/`offset` to `list_datasets` (**P2-2**), matching `query_sql`'s existing caps.~~ **Done — `c7def9c`.**

**Testing process**
6. **The fixture-drift pattern is the most transferable finding.** A green unit suite asserted behaviour that is broken against the real API. Recommend a small contract-test tier that runs against recorded live responses, refreshed periodically — the parser was correct; only the assumed response shape was wrong, and no amount of unit testing against hand-written fixtures would ever catch that.
7. Add a multi-process concurrency test (serve + sync simultaneously). P1-1 is invisible to single-process tests, which is likely why it survived.
8. Consider surfacing upstream data-quality signals. Cook County's `sentence_date` ranges to the year **2921**, and `race` carries both `HISPANIC` and `White [Hispanic or Latino]` as distinct values. Not `csq` bugs — but an agent querying this data will produce confidently wrong answers, and a `csq doctor`-style column profile would help consumers catch it.

**Not yet covered** — snapshot/fetch, write tools, HTTP transport. Recommend a follow-up session.

---

## Appendix — environment notes

- MCP registration and the stdio handshake both succeeded on first attempt; `claude mcp list` reported `✔ Connected`.
- `.gitignore` covered `*.duckdb` but not `*.duckdb.lock`, leaving advisory lock files untracked in `git status`. Fixed in commit `c3cbb4c`.
- Cook County config committed as `datacatalog.cookcountyil.gov.yaml` (`c3cbb4c`), five datasets set `mode: full_replace` because SAO rows are mutable and republished wholesale — incremental append on `:updated_at` would accumulate stale duplicates.

---

## Revision history

This report is amended in place rather than reissued, so findings keep stable identifiers (P1-1, P2-1, …) across versions. Every substantive change is listed here, including corrections to the report's own earlier claims.

### Rev 4 — 2026-08-07

**P1-2 fixed (`95d56b3`) and P2-2 fixed (`c7def9c`).** Both High-severity defects are now closed; 4 of 7 overall.

**Both were encoded in the test suite, not missed by it.** P1-2's synthesised `table_name` was asserted as correct by `TestListDatasets_NeverSynced_RowCountNil`, under a comment naming it a deliberate fallback. This is a distinct failure mode from P2-1's fixture drift: there the test held a wrong belief about the *API*, here it held a wrong belief about the *requirement*. Running the suite more often, on more machines, against fresher fixtures, would never have caught it.

**Three defects, three different testing failures.** Worth stating together, because the remedy differs for each: P2-1 was fixture drift (fix: contract tests against recorded real responses); P1-2 was a wrong requirement written into an assertion (fix: review what tests *assert*, not just whether they pass); P1-1 was an untested cross-process property (fix: add multi-process tests). No single practice covers all three.

**P1-2 was also present in `describe_dataset`**, where it additionally drove a lookup against a table that never existed. A defect found in one handler is worth grepping for across siblings — the original report described it as a `list_datasets` bug and it was not.

**Scope note.** `csq query` (`a100a6a`) and paging both came out of the accessibility review rather than this report. The absence of any export path — no CSV, no Parquet, no `COPY` anywhere in the tree — is arguably a more serious limitation for a research tool than any single defect filed here, and should be filed properly in a future session rather than left as an aside.

### Rev 3 — 2026-08-07

**P1-1 fixed (`a100a6a`).** `OpenPools` takes a writable-alias set and opens every other portal `access_mode=read_only`.

**Correction — P1-1's central impact claim was wrong, and severity is downgraded to Medium.** Rev 1 asserted the fix would allow a `csq sync` to run while a portal is served. Measurement disproved it: DuckDB permits many readers or one writer, never both, so a writer is refused whichever mode the reader holds. Serve-while-syncing is unreachable by access-mode changes and needs snapshot-and-swap. The blocked workflow that justified "High" does not exist; what the fix genuinely delivers is reader-reader concurrency (two servers, or a server plus a CLI session).

**Engine-version caveat recorded.** csq embeds DuckDB v1.4.3 while the standalone CLI used for verification is v1.5.5, and the two differ on reader/writer overlap. That gap explains why some earlier observations in this report looked contradictory, and any future concurrency claim should name the version it was measured on.

**Pattern worth noting across both fixes so far.** P2-1 and P1-1 were each real defects whose *stated consequence* was wrong — one overstated the benefit of fixing, the other the harm of leaving. Both were only caught by implementing the fix and measuring. Recommend treating a defect's impact claim as unverified until the fix is exercised, particularly where it rests on assumed behaviour of a dependency.

**Not a defect fix, but related:** `csq query` was added in the same commit, giving read-only ad-hoc SQL and `--format table|csv|json|parquet`. Before it, csq had no way to run arbitrary SQL and no export path of any kind — a gap this session hit repeatedly and which is arguably more limiting for a research audience than any single defect listed here. Worth considering as a filed item in a future report rather than an incidental.

### Rev 2 — 2026-08-06

**P2-1 fixed (`68ad3f2`).** `rawCatalogEntry` now reads `data_updated_at` with fallbacks to `updatedAt` and `rowsUpdatedAt`. Verified live: Cook County `_csq.catalog.updated_at` went from 0/808 populated to 808/808. The test fixture that hid the bug was replaced with a real response shape, and two tests were added (`TestCatalogTimestampPrecedence`, `TestFetchCatalog_LiveResponseShape`).

**Correction — P2-1's impact was overstated in Rev 1.** The original entry claimed a working `updated_at` "would have immediately flagged that all five Cook County datasets were frozen." Implementing the fix disproved that. Those datasets were abandoned on 2024-12-30 but now report `updated_at` of 2026-04-02, because the portal republished unchanged rows. The fix closes the metadata-edit hole, not the republish hole; `data_updated_at` is the correct field but remains an insufficient staleness signal on its own. The entry has been rewritten to say so, and the project README's freshness caveat was rewritten to match rather than left overstating the outcome.

**New finding recorded during the fix.** `updatedAt` is `max(data_updated_at, metadata_updated_at)` — measured across a 100-dataset Chicago sample it was later than `data_updated_at` 48 times and earlier zero times. The cheap one-word fix (retarget the tag at `updatedAt`) would have cleared the NULLs while reporting metadata churn as data freshness. Recorded because it argues for verifying a field's *semantics*, not just its presence, whenever a struct tag is corrected.

**Recommendation 4 partially closed.** The fix half is done; the audit half — checking the remaining `internal/socrata` struct tags against the live API — still stands and is unchanged in priority.

**Status line added** to the header: 1 of 7 defects fixed; P1-1 and P1-2 remain open and remain the two that matter.

### Rev 1 — 2026-08-06

Initial report. Seven defects (P1-1, P1-2, P2-1, P2-2, P3-1, P3-2, P3-3), five successes, four operator errors disclosed, eight recommendations.

---

## Related work since Rev 1

Not defect fixes, but relevant context if this report is read alongside the repository:

- **`internal/modes/`** (`e532838`, `b6589f4`) — curated analysis profiles surfaced at startup, including a `research` mode that automates parts of the due-diligence this session did by hand. Its `modes run` path attaches every portal `READ_ONLY` and demonstrably coexists with a running MCP server holding the same file, which makes it a working in-repo counterexample to **P1-1** and lowers the risk of that fix.
- **P1-1 second-layer observation — since qualified.** With the DuckDB CLI, `duckdb -readonly <portal>.duckdb` succeeds against a file the MCP server holds while the same command without `-readonly` fails with the conflicting-lock error. Rev 1 read this as evidence that read-only pools would resolve the contention outright. It does not support that conclusion: the two processes run different engine versions (csq v1.4.3, CLI v1.5.5) which differ on reader/writer overlap, and a same-version pairing behaves differently. It remains a fair demonstration that *read-only access is the right privilege level for a serving process* — just not proof about concurrency. See P1-1's Resolution for the measured rule.
- **`csq query`** (`a100a6a`) — read-only ad-hoc SQL with `--format table|csv|json|parquet`. Relevant to this report because several findings above were only reachable by hand-writing a JSON-RPC harness against the MCP server or installing DuckDB separately; a future QA session has a supported path.
